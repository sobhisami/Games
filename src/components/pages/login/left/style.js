import { styled } from "styled-components"

export const StyledBrand = styled.div`
 margin-bottom : 8.75rem;
 margin-top : 4.25rem;
`
export const JoyImage = styled.img`
  position: absolute;
  bottom : 0;
  right : 0;
  transform: scale(0.8);
`